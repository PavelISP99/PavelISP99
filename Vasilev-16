#include <iostream>
#include <windows.h>
#define color system("color 53")
#define text system("color 0B");
#define clear system("cls");
using namespace std;


int main() {
	setlocale(0, "");
	cout << "[+] Ïðîãðàììà - Ôóíêöèè \n";
	cout << "[1] î÷èùåíèå êîíñîëè \n";
	cout << "[2] èçìåíèòü öâåò ôîíà \n";
	cout << "[3] èçìåíèòü öâåò òåêñòà \n";
	cout << "[4] âûâîä ðàçìåðà òèïà äàííûõ int \n";
	cout << "[5] âûâîä ðàçìåðà òèïà äàííûõ char \n";
	cout << "[6] âûâîä ðàçìåðà òèïà äàííûõ double \n";
	cout << "[7] âûâîä ðàçìåðà òèïà äàííûõ float \n";
	cout << "[8] âûâîä ðàçìåðà òèïà äàííûõ bool \n";
	cout << "[9] âûâîä ñîîáùåíèÿ ïðèâåò ÿ ôóíêöèÿ \n";
	cout << "[+] Ââåäèòå öèôðó \n";



	int menu;
	cin >> menu;

	if (menu == 1) {
		cout << " Î÷èùåíèå êîíñîëè \n";
		clear;
	}

	if (menu == 2) {
		clear;
		cout << " èçìåíåíèå öâåòà ôîíà \n";
		color;
		Sleep(2000);
		main();
	}
	if (menu == 3) {
		clear;
		cout << " èçìåíåíèå öâåòà òåêñòà \n";
		text;
		Sleep(2000);
		main();
	}
	if (menu == 4) {
		clear;
		cout << " âûâîä ðàçìåðà òèïà äàííûõ int \n";
		int tip1 = 10;
		cout << "int | tip1 = " << sizeof(tip1) << endl;
	}

	if (menu == 5) {
		clear;
		cout << " âûâîä ðàçìåðà òèïà äàííûõ char \n";
		char tip2 = '#';
		cout << "char | tip2 = " << sizeof(tip2) << endl;
	}

	if (menu == 6) {
		clear;
		cout << " âûâîä ðàçìåðà òèïà äàííûõ double \n";
		double tip3 = 1.2;
		cout << "double | tip3 = " << sizeof(tip3) << endl;
	}

	if (menu == 7) {
		clear;
		cout << " âûâîä ðàçìåðà òèïà äàííûõ float \n";
		float tip4 = 122.2;
		cout << "float |tip4 = " << sizeof(tip4) << endl;
	}

	if (menu == 8) {
		clear;
		cout << " âûâîä ðàçìåðà òèïà äàííûõ bool \n";
		bool tip5 = true;
		cout << "bool | ti5 = " << sizeof(tip5) << endl;
	}
	if (menu == 9) {
		clear;
		cout << "Ïðèâåò ÿ ôóíêöèÿ!" << endl;
	}


	int _; cin >> _;
	return 0;
}
